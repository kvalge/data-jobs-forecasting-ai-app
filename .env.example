# Required for CLI and web (non-empty): OPENROUTER_API_KEY, DATABASE_URL, MODEL, FALLBACK_MODEL.
# Optional: FALLBACK_MODEL2 / FALLBACK_MODEL3 (tried when earlier OpenRouter models fail / hit limits).
# Optional local fallback: Ollama (used only after all OpenRouter models fail).
# Web: SECRET_KEY (required unless FLASK_ENV=development), FLASK_HOST, FLASK_DEBUG, FLASK_ENV.
# PREDICTION_DATA_SOURCE.
OPENROUTER_API_KEY=apikey
MODEL=modelname
FALLBACK_MODEL=modelname
FALLBACK_MODEL2=modelname
FALLBACK_MODEL3=modelname
DATABASE_URL=databaseurl
# Long random value required for web unless FLASK_ENV=development (placeholders are rejected).
SECRET_KEY=replace-with-a-long-random-string
FLASK_ENV=development
FLASK_HOST=127.0.0.1
FLASK_DEBUG=false
# Max characters of posting text sent to OpenRouter/Ollama (default 100000).
MAX_POSTING_CHARS=100000
# After OpenRouter free-tier / all models fail, try local Ollama (default on).
OLLAMA_FALLBACK_ENABLED=true
OLLAMA_BASE_URL=baseurl
OLLAMA_MODEL=modelname
OLLAMA_TIMEOUT_SECONDS=180
# Prediction series: fake (data/fake CSVs) or database (not implemented yet).
PREDICTION_DATA_SOURCE=fake
